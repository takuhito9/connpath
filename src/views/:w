<template>
  <div style="position: relative">
    <template v-if="!editMode">
      <template v-if="isCosExist">
        <button class="button_positive" @click="toInputMode">
          <i
            class="material-icons"
            style="margin-right: 5%; vertical-align: -5px"
            add_solution_input
          >
            add_circle
          </i>
        </button>

        <p>データが存在する場合</p>
      </template>
      <template v-else>
        <p>データが存在しない場合</p>
      </template>
    </template>
    <template v-else>
      <p>追加buttonを押した</p>
    </template>
  </div>
</template>

<script lang="ts">
import Vue from "vue";
export default Vue.extend({
  data() {
    return {
      editMode: false,
    };
  },
  props: {
    isCosExist: { type: Boolean },
  },
});
</script>
<style lang="scss" scoped>
</style>
